# store custom template at db/database.yml.erb
set :template_dir, "db"

production:
  adapter: mysql2
  encoding: utf8
  reconnect: false
  database: guaguale_production
  pool: 5
  username: root
  password: #{Capistrano::CLI.ui.ask("Enter MySQL database password: ")}
  host: localhost

